Goal: connect to the slide: arrays are fixed length, Lists are resizable and ordered.

1) Implement a method removeAllOccurrences(List<String> xs, String target).
   Compare performance intuition: ArrayList vs LinkedList (no benchmarking required).

2) Demonstrate positional access (get/set/add/remove) and searching (indexOf/lastIndexOf)
   using a small sample list.

3) Create a subList view and modify it. Observe what happens to the original list.
