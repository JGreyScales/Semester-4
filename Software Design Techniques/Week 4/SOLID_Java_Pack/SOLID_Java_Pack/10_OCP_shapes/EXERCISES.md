Goal: open for extension, closed for modification.

1) Add a new shape (e.g., Rectangle) that implements Shape.
   Do NOT edit AreaCalculator.

2) Create a bad calculator method totalAreaBad(Object[] shapes) that checks types using instanceof.
   Show (in comments) why it keeps changing when new shapes are added.

3) Optional: introduce a ShapeFactory that parses strings like "square:3" into Shape objects.
