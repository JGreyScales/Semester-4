Goal: a class/method should have ONE reason to change.

1) Create a BadEmployee class that does:
   doWork(), saveEmployee(), deleteEmployee() inside the same class.
   In comments, list at least 2 different reasons it might change.

2) Extend EmployeeRepository to support updateEmployee(Employee e).
   Keep Employee unchanged.

3) Create a small class diagram showing Employee and EmployeeRepository responsibilities.
