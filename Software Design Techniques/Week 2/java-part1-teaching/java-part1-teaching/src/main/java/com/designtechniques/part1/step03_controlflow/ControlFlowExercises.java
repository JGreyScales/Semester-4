package com.designtechniques.part1.step03_controlflow;

/**
 * Student exercises for Step 03.
 */
public final class ControlFlowExercises {
    private ControlFlowExercises() {}

    /**
     * Task: Return "Fizz" if n divisible by 3, "Buzz" if divisible by 5,
     * "FizzBuzz" if divisible by both, otherwise return the number as a String.
     */
    public static String fizzBuzz(int n) {
        // TODO (student): implement using if/else.
        return "TODO";
    }

    /**
     * Task: Count how many integers in [start, end] are even.
     * If start > end, swap them.
     */
    public static int countEvensInclusive(int start, int end) {
        // TODO (student): implement using a for-loop.
        return -1;
    }
}
